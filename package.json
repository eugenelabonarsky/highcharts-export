{
  "name": "highcharts-export",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "npm install && highcharts-export-server --enableServer 1 --port ${PORT:=3000}",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "highcharts-export-server": "1.0.15"
  }
}
