{
  "name": "highcharts-export",
  "version": "0.0.1",
  "scripts": {
    "start": "highcharts-export-server --enableServer 1 --port ${PORT:=5000}",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "dotenv": "4.0.0",
    "highcharts-export-server": "1.0.14"
  }
}
