{
  "name": "highcharts-export",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "node node_modules/highcharts-export-server/build.js && highcharts-export-server --enableServer 1 --port ${PORT:=5000}",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "4.0.0",
    "highcharts-export-server": "1.0.15"
  }
}
